HTTP Server
-----------

you can change the address in the source code to use.
it can send only a small data, you can change it from source code.

compile:
    gcc http_server.c -o http_server

usage:
    ./http_server
    open your browser and go to the specified host-address/port
